<%= link "new search", to: "/" %>
<h2> <%= @data["total"] %> results for "<%= @search %>"</h2>
<ul>
  <%= for evidence <- @data["documents"] do %>
    <li><%= link evidence["title"], to: evidence["link"] %> </li>
  <% end %>
</ul>

